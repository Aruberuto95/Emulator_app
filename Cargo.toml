[workspace]
members = [
    "core",
    "third_party/n64-engine",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
# The N64 boundary catches guest faults before returning through the C++ ABI.
panic = "unwind"

[profile.dev]
opt-level = 3
codegen-units = 1
