[package]
name = "emulator_core"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"

[lib]
name = "emulator_core"
crate-type = ["staticlib", "rlib"]

[dependencies]
cxx = "1.0"

[build-dependencies]
cxx-build = "1.0"

